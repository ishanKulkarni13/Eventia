# Eventia

Eventia is a role-based campus event platform built with React and Express.

Roles:
- Admin: create/manage events, assign volunteers, view registered students.
- Volunteer: view assigned events.
- Student: browse events and register (capacity enforced).

Authentication uses JWT, and all permissions are enforced on the backend.